# macOS CLI environment

- The primary shell is zsh on Apple silicon macOS.
- Prefer portable BSD-compatible command invocations unless GNU behavior is
  explicitly available.
- fzf, fd, tmux, git, gh, just, and Docker are common interactive tools.
- Generated commands must be inspectable and must not execute destructive
  actions automatically.
